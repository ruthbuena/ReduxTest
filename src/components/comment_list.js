// functional component - stateless, no need for lifecycle methods
import React from 'react';

const CommentList = (props) => {
  return (
      <ul className="comment-list"></ul>
  );
};

export default CommentList;
